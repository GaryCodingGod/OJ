#include <iostream>
using namespace std;

int main(){//
    /*class Segment{
    
    private:
        Point * p1 = NULL;
  		Point * p2 = NULL;
    public:
        Segment(Point p1s,Point p2s){
            p1 = &p1s;
            p2 = &p2s;
        }
        double length(){
            return sqrt((pow(p1->getX()-p2->getX(),2)) + pow(p1->getY() - p2->getY(),2));
        }
        Point getEnd(int value){
            if(value == 0)
                return *p1;
            else
                return *p2;
        }
    
};*/
}